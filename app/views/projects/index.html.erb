<h1>專案頁面</h1>

<div class="container">
	<% @projects.each do |p| %>
		<table>
			
			<tr>
				<td>
					<%= p.name %><%= link_to "show", project_path(p)   %>
				</td>
			</tr>

		</table>
	<% end %>

</div>
