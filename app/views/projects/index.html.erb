<div class="contaienr text-center">
	<h1>募資中專案</h1>
	<small>每個人的一點心力就能匯聚成眾大的公益力量，和我們ㄧ起行動!</small> 
	<div class="container">
		<%= image_tag "donation.png" %>
	</div>
	<div class="container">
		<p><i>依分類搜尋:</i>&nbsp;&nbsp;&nbsp;
		<% Tag.all.each do |t| %>
			<i class="fa fa-tag " aria-hidden="true"><%= link_to t.name, tag_path(t.id)%>&nbsp;&nbsp;</i>						
		<% end %>
		</i></p>
	</div>
</div>

<div class="container">
<row>
<% @projects.each do |p| %>
	<div class="col-md-6 ">
	<div class="col-md-12">
		<%= link_to image_tag(p.photos.first.image.url,class:"top")  %>
	</div>					
	<div class="col-md-12 text-center">
		<h3><%=truncate(p.name, length: 20)%></strong></h3>	
		<br>
		<div class="progress">
			<% @progress = p.progress.round(0) %>
	  	<div class="progress-bar pb" role="progressbar" style="width: <%= @progress %>%" aria-valuenow="<% @progress %>" aria-valuemin="0" aria-valuemax="100"><%= @progress %> %</div>
	  </div>
		
		<%= link_to "查看", project_path(p),class:"btn-2nd" %>	<br><br>	
	</div>
	</div>

	<% end %>			
</row>
</div>
<style>
	.container{
		margin-top: 20px;
	}
</style>

<script>
	$('.pb').css({
    'background-image': 'none',
    'background-color': '#fa7f7f'
});
</script>