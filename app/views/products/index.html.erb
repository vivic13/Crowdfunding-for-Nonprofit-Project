<h1>產品首頁</h1>

<div class="container">
	<% @products.each do |p| %>
		<table>
			
			<tr>
				<td>
					<%= p.name %><%= link_to "show", product_path(p)   %>
				</td>
			</tr>

		</table>
	<% end %>

</div>